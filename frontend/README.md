# Frontend

The real React or Next.js app goes here.

The current root `index.html`, `styles.css`, and `script.js` files are a clickable prototype. Later, this folder should replace that prototype with a real frontend connected to the backend API.
